query GetSettings {
  settings {
    isReady
    availableDevices
    modelsFilePath

    device
    showPreviews
    stepsPerPreview
    useFullPrecision
    models {
      isActive
      id
      name
      weightsFilename
      configFilename
      width
      height
    }
  }
}
